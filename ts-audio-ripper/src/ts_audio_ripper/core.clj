(ns ts-audio-ripper.core
  (:use [clj-audio.core] :reload-all)
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
;  (play (->stream "~/test-ts3.wav"))
  (println "Hello, World!"))
  
